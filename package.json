{
  "name": "reasonova",
  "version": "1.0.0",
  "scripts": {
    "start": "npm run dev",
    "dev": "npm run dev:frontend & npm run dev:backend",
    "dev:frontend": "cd frontend && npm run dev",
    "dev:backend": "cd backend/functions && npm run dev",
    "start:frontend": "cd frontend && npm run dev",
    "start:backend": "cd backend/functions && npm start",
    "build": "npm run build:frontend && npm run build:backend",
    "build:frontend": "cd frontend && npm run build",
    "build:backend": "cd backend/functions && npm run build",
    "build:buildfunctions": "cd backend/functions && npm run build",
    "generate-pdf-slices": "node scripts/generatePdfQuestionSlices.js",
    "deploy": "firebase deploy"
  },
  "dependencies": {
    "firebase": "^12.10.0"
  },
  "devDependencies": {
    "pdf-lib": "^1.17.1"
  }
}
